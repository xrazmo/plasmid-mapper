name: plasmid-mapper
channels:
  - bioconda
  - conda-forge
dependencies:
  - python>=3.10
  - prokka>=1.14.6  # older prokka (e.g. 1.13) misparses modern blastp version strings (e.g. "2.17") as too old
  - blast>=2.12
  - biopython>=1.79
  - pip
  - pip:
      - -e .
